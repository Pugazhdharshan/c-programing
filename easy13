#include <stdio.h>

int main() {
  int i, n, count = 0;
  int arr[] = {16, -18, 27, -16, 23, -21, 19};
  n = sizeof(arr) / sizeof(arr[0]);

  for (i = 0; i < n; i++) {
    if (arr[i] < 0) {
      count++;
    }
  }

  printf("The number of negative numbers is: %d\n", count);

  return 0;
}
