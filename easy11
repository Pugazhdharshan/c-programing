#include <stdio.h>

int main() {
  int m, n, i, result;
  printf("Enter the value of m: ");
  scanf("%d", &m);
  printf("Enter the value of n: ");
  scanf("%d", &n);

  for (i = 1; i <= n; i++) {
    result = m * i;
    printf("%d x %d = %d\n", m, i, result);
  }

  return 0;
}
