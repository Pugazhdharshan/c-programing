#include <stdio.h>

int square(int num) {
  return num * num;
}

int main() {
  int num;
  printf("Input any number for square: ");
  scanf("%d", &num);

  printf("Square of %d is %d\n", num, square(num));

  return 0;
}
