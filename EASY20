#include <stdio.h>
#include <string.h>

int main() {
  char str[100];
  int length;
  char *ptr;

  printf("Input a string: ");
  scanf("%s", str);

  ptr = str;
  length = 0;
  while (*ptr != '\0') {
    length++;
    ptr++;
  }

  printf("Length of the string: %d\n", length);
  return 0;
}
